import 'package:flutter/material.dart';

class location_weather_page extends StatefulWidget {
  const location_weather_page({Key? key}) : super(key: key);

  @override
  State<location_weather_page> createState() => _location_weather_pageState();
}

class _location_weather_pageState extends State<location_weather_page> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("kk"),
      ),
    );
  }
}
